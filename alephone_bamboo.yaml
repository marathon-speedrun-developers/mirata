name: alephone
version: git
summary: AlephOne
description: "Aleph One is a source port of based on the source code for Bungie's Marathon 2 Engine."

confinement: mode

apps:
    alephone:
        command: alephone
        plugs:
          - home
          - joystick

parts:
    alephone:
        plugin: autotools
        source: '.'
        configflags:
          - --without-ffmpeg
        build-packages:
          - libboost-all-dev
          - libsdl1.2-dev
          - libsdl-image1.2-dev
          - libsdl-net1.2-dev
          - libsdl-ttf2.0-dev
          - libspeexdsp-dev
          - libzzip-dev
          - libavcodec-dev
          - libavformat-dev
          - libavutil-dev
          - libswscale-dev
          - zlib1g-dev
        stage-packages:
          - libc6
          - zlib1g
          - libboost-all
          - libsdl1.2
          - libsdl-image1.2
          - libsdl-net1.2
          - libsdl-ttf2.0
          - libspeexdsp
          - libzzip
          - libavcodec
          - libavformat
          - libavutil
          - libswscale
